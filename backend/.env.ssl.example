# MaxLab 데이터베이스 SSL/TLS 설정 예시
# 프로덕션 환경에서 사용할 SSL 설정

# 기본 데이터베이스 설정
DATABASE_URL=postgresql+asyncpg://username:password@hostname:5432/database

# SSL/TLS 보안 설정
# SSL 모드: disable, allow, prefer, require, verify-ca, verify-full
DB_SSL_MODE=require

# 클라이언트 인증서 경로 (상호 TLS 인증을 위한 설정)
DB_SSL_CERT_PATH=/path/to/client.crt
DB_SSL_KEY_PATH=/path/to/client.key

# CA 인증서 경로 (서버 인증서 검증을 위한 설정)
DB_SSL_CA_PATH=/path/to/ca.crt

# 프로덕션 환경 설정
ENVIRONMENT=production
DEBUG=false

# 보안 키 (프로덕션에서 반드시 변경)
SECRET_KEY=your-super-secret-key-change-this-in-production
JWT_SECRET_KEY=your-jwt-secret-key-change-this-in-production

# SSL 모드 설명:
# - disable: SSL 비활성화
# - allow: SSL 사용 가능하면 사용, 아니면 일반 연결
# - prefer: SSL 우선 시도, 실패하면 일반 연결 (기본값)
# - require: SSL 강제, 인증서 검증 안함
# - verify-ca: SSL 강제, CA 인증서 검증
# - verify-full: SSL 강제, CA 인증서 + 호스트명 검증 (권장)

# 개발 환경에서의 자체 서명 인증서 사용 예시:
# DB_SSL_MODE=require
# DB_SSL_CERT_PATH=./ssl_certs/localhost.crt
# DB_SSL_KEY_PATH=./ssl_certs/localhost.key