# MaxLab 스테이징 환경 설정 템플릿
# 스테이징 환경에서 사용할 설정 템플릿

# 환경 설정
ENVIRONMENT=staging

# 데이터베이스 설정
DATABASE_URL=postgresql+asyncpg://username:password@staging-db-host:5432/max_lab_staging

# SSL 설정 (스테이징 환경에서는 prefer)
# DB_SSL_MODE=prefer

# 보안 키 (스테이징용 - 운영과 다른 값 사용)
SECRET_KEY=STAGING_SECRET_KEY_CHANGE_THIS
JWT_SECRET_KEY=STAGING_JWT_SECRET_KEY_CHANGE_THIS
CSRF_SECRET_KEY=STAGING_CSRF_SECRET_KEY_CHANGE_THIS
SESSION_SECRET_KEY=STAGING_SESSION_SECRET_KEY_CHANGE_THIS

# 암호화 키 (스테이징용)
ENCRYPTION_KEY=STAGING_ENCRYPTION_KEY_CHANGE_THIS

# 외부 인증 서버
AUTH_SERVER_URL=https://staging-auth.maxlab.com
AUTH_SERVER_TIMEOUT=20.0

# 서버 설정
HOST=0.0.0.0
PORT=8010

# CORS 설정 (스테이징 도메인)
CORS_ALLOW_ORIGINS=https://staging.maxlab.com,https://staging-api.maxlab.com

# Redis 설정 (스테이징 환경)
REDIS_URL=redis://staging-redis:6379/0

# WebSocket Configuration
ENABLE_WEBSOCKET=true
WEBSOCKET_TOKEN=STAGING_WS_TOKEN_CHANGE_THIS

# MSSQL Configuration (for data providers)
MSSQL_CONNECTION_STRING=DRIVER={ODBC Driver 17 for SQL Server};SERVER=staging-sql-server;DATABASE=equipment_db_staging;UID=username;PWD=password;Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30;Command Timeout=60

# Scheduler
AUTO_CLOSE_DAYS_DEFAULT=90